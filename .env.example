# =============================================================================
# Agora - Konfiguration
# =============================================================================
# Kopiere diese Datei nach .env und passe die Werte an:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Datenbank (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_DB=agora
POSTGRES_USER=agora
POSTGRES_PASSWORD=agora_secret
# POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Sicherheit
# -----------------------------------------------------------------------------
# WICHTIG: In Produktion unbedingt aendern!
JWT_SECRET=change-me-in-production-use-a-real-secret

# -----------------------------------------------------------------------------
# Google Kalender (OAuth2)
# -----------------------------------------------------------------------------
# Anleitung: siehe README.md -> "Google Kalender einrichten"
# Ohne diese Werte ist die Google-Kalender-Integration deaktiviert.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# E-Mail (SMTP)
# -----------------------------------------------------------------------------
# Im Entwicklungsmodus wird MailHog verwendet (kein SMTP noetig).
# Fuer Produktion hier einen echten SMTP-Server konfigurieren.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=deine-email@gmail.com
# SMTP_PASSWORD=dein-app-passwort
# SMTP_FROM=noreply@deine-domain.de
# SMTP_USE_TLS=true

# -----------------------------------------------------------------------------
# Frontend-URL
# -----------------------------------------------------------------------------
# URL unter der das Frontend erreichbar ist (fuer OAuth-Callbacks und E-Mail-Links).
# In Docker (nginx): http://localhost (Port 80)
# Ohne Docker (ng serve): http://localhost:4200
# Produktion: https://agora.deine-domain.de
FRONTEND_URL=http://localhost

# -----------------------------------------------------------------------------
# LDAP / Active Directory (optional)
# -----------------------------------------------------------------------------
# LDAP_ENABLED=true
# LDAP_SERVER=ldap://dein-dc.firma.local
# LDAP_PORT=389
# LDAP_USE_SSL=false
# LDAP_BIND_DN=CN=ldap-reader,OU=Service,DC=firma,DC=local
# LDAP_BIND_PASSWORD=geheim
# LDAP_BASE_DN=OU=Benutzer,DC=firma,DC=local
# LDAP_USER_FILTER=(sAMAccountName={username})
# LDAP_GROUP_DN=CN=Agora-Users,OU=Gruppen,DC=firma,DC=local
# LDAP_USERNAME_ATTR=sAMAccountName
# LDAP_EMAIL_ATTR=mail
# LDAP_DISPLAY_NAME_ATTR=displayName
# LDAP_ADMIN_GROUP_DN=CN=Agora-Admins,OU=Gruppen,DC=firma,DC=local

# -----------------------------------------------------------------------------
# Janus WebRTC Gateway
# -----------------------------------------------------------------------------
# JANUS_API_SECRET=janus-api-secret

# -----------------------------------------------------------------------------
# Ports (optional - nur aendern bei Konflikten)
# -----------------------------------------------------------------------------
# BACKEND_PORT=8000
# REDIS_PORT=6379
# HTTP_PORT=80
# HTTPS_PORT=443

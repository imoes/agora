# =============================================================================
# Agora - Configuration
# =============================================================================
# Copy this file to .env and adjust the values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_DB=agora
POSTGRES_USER=agora
POSTGRES_PASSWORD=agora_secret
# POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
# IMPORTANT: Change this in production!
JWT_SECRET=change-me-in-production-use-a-real-secret

# -----------------------------------------------------------------------------
# Google Calendar (OAuth2)
# -----------------------------------------------------------------------------
# Instructions: see README.md -> "Google Calendar Setup"
# Without these values, the Google Calendar integration is disabled.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Email (SMTP)
# -----------------------------------------------------------------------------
# In development mode, MailHog is used (no SMTP needed).
# For production, configure a real SMTP server here.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@your-domain.com
# SMTP_USE_TLS=true

# -----------------------------------------------------------------------------
# Frontend URL
# -----------------------------------------------------------------------------
# URL where the frontend is accessible (used for OAuth callbacks and email links).
# Docker (nginx): http://localhost (port 80)
# Without Docker (ng serve): http://localhost:4200
# Production: https://agora.your-domain.com
FRONTEND_URL=http://localhost

# -----------------------------------------------------------------------------
# LDAP / Active Directory (optional)
# -----------------------------------------------------------------------------
# LDAP_ENABLED=true
# LDAP_SERVER=ldap://your-dc.company.local
# LDAP_PORT=389
# LDAP_USE_SSL=false
# LDAP_BIND_DN=CN=ldap-reader,OU=Service,DC=company,DC=local
# LDAP_BIND_PASSWORD=secret
# LDAP_BASE_DN=OU=Users,DC=company,DC=local
# LDAP_USER_FILTER=(sAMAccountName={username})
# LDAP_GROUP_DN=CN=Agora-Users,OU=Groups,DC=company,DC=local
# LDAP_USERNAME_ATTR=sAMAccountName
# LDAP_EMAIL_ATTR=mail
# LDAP_DISPLAY_NAME_ATTR=displayName
# LDAP_ADMIN_GROUP_DN=CN=Agora-Admins,OU=Groups,DC=company,DC=local

# -----------------------------------------------------------------------------
# Janus WebRTC Gateway
# -----------------------------------------------------------------------------
# JANUS_API_SECRET=janus-api-secret

# -----------------------------------------------------------------------------
# Ports (optional - only change if there are conflicts)
# -----------------------------------------------------------------------------
# BACKEND_PORT=8000
# REDIS_PORT=6379
# HTTP_PORT=80
# HTTPS_PORT=443
